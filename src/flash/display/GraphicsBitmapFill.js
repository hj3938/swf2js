/**
 * @param bitmapData
 * @param matrix
 * @param repeat
 * @param smooth
 * @constructor
 */
var GraphicsBitmapFill = function (bitmapData, matrix, repeat, smooth)
{
    this._bitmapData = null;
    this._matrix     = null;
    this._repeat     = true;
    this._smooth     = false;
};
